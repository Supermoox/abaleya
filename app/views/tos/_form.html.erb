<%= form_with(model: to, local: true) do |form| %>
  <% if to.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(to.errors.count, "error") %> prohibited this to from being saved:</h2>

      <ul>
      <% to.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :to_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

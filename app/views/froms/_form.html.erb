<%= form_with(model: from, local: true) do |form| %>
  <% if from.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(from.errors.count, "error") %> prohibited this from from being saved:</h2>

      <ul>
      <% from.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :from_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
